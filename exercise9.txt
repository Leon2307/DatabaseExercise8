1.
SELECT name
FROM Products
WHERE price IN (
	SELECT price
	FROM Products
	GROUP BY price
	HAVING COUNT(*) = 1
)

2.
SELECT MIN(word) as word
FROM Words

3.
SELECT word
FROM Words
WHERE word NOT IN (
	SELECT MIN(word)
	FROM Words
	)

4.
SELECT word
FROM Words
WHERE word LIKE '%i%'

5.
